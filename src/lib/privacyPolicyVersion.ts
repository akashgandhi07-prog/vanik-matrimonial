/**
 * Stored on successful registration (`member_private.consent_privacy_policy_version`).
 * Keep in sync with `supabase/functions/_shared/privacy-policy-version.ts` (`DEFAULT_PRIVACY_POLICY_VERSION_ID`).
 */
export const PRIVACY_POLICY_VERSION_ID = '2026-05-11';

/** Must match visible “Last updated” on `/privacy`. */
export const PRIVACY_POLICY_LAST_UPDATED_DISPLAY = '11 May 2026';
